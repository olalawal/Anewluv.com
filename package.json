{
  "name": "anewluv-marketing-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build && node scripts/local-preview-server.mjs",
    "dev:vite": "vite --host 127.0.0.1 --port 8082",
    "build": "vite build",
    "preview": "npm run build && node scripts/local-preview-server.mjs",
    "prepare:preview-account": "node scripts/prepare-preview-account.mjs"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "vite": "^7.0.0",
    "typescript": "^5.8.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {}
}
